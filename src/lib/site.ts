// The production origin, used for canonical URLs, JSON-LD @ids and absolute
// links in generated documents. One constant so a domain move touches one line
// (the Worker has its own copies — it cannot import from src/).
export const SITE_ORIGIN = "https://murugappan.dev";
