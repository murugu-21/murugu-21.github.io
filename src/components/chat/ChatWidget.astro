---
// React island wrapper. client:idle defers hydration (React + widget JS) to
// browser idle so it stays out of the initial load waterfall; the wrapper div
// carries the id the chat.css design tokens are scoped under.
import {ChatWidget} from "./ChatWidget";
---

<div id="chat-widget-root">
  <ChatWidget client:idle />
</div>
