---
import Icon from "./Icon.astro";
---

<button id="topButton" aria-label="Back to top" title="Go to top">
  <Icon name="hand-point-up" />
</button>

<style lang="scss">
  @import "../styles/variables";

  /* from old containers/topbutton/Top.scss; the old JS toggled visibility,
     hidden by default — base state is display: none here instead */
  #topButton {
    display: none;
    position: fixed;
    bottom: 20px;
    right: 30px;
    z-index: 99;
    border: none;
    outline: none;
    background-color: $buttonColor;
    color: white;
    cursor: pointer;
    padding: 15px;
    border-radius: 15px;
    font-size: 25px;
  }

  /* the FA <i> glyph was sized by font-size: 25px with line-height 1;
     display: block reproduces the exact 25px content box without a baseline gap */
  #topButton :global(svg) {
    width: 25px;
    height: 25px;
    display: block;
  }

  #topButton:hover {
    background-color: $topButtonHover;
    transition: all ease-in-out 0.2s;
  }
</style>

<script>
  const btn = document.getElementById("topButton")!;
  window.addEventListener(
    "scroll",
    () => {
      btn.style.display = window.scrollY > 20 ? "block" : "none";
    },
    {passive: true}
  );
  btn.addEventListener("click", () =>
    window.scrollTo({top: 0, behavior: "smooth"})
  );
</script>
