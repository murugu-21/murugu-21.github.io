---
// Shared React chat widget from the portfolio app (see ../../../src/components
// /chat/ChatWidget.tsx). client:idle defers hydration to browser idle; the
// wrapper div carries the id the chat.css design tokens are scoped under.
import { ChatWidget } from "../../../src/components/chat/ChatWidget"
---

<div id="chat-widget-root">
  <ChatWidget client:idle />
</div>
