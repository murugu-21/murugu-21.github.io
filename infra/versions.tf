terraform {
  required_version = ">= 1.6"

  required_providers {
    cloudflare = {
      source  = "cloudflare/cloudflare"
      version = "~> 5.0"
    }
  }
}

# Auth via CLOUDFLARE_API_TOKEN environment variable.
provider "cloudflare" {}
